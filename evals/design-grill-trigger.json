[
  {"query": "right so we need a way for admins to bulk-invite users by uploading a csv. there's already a single-invite endpoint in src/api/invites.ts, i'm thinking we reuse most of it but i'm not sure how to handle partial failures", "should_trigger": true},
  {"query": "the InvoiceTotals class has grown to about 600 lines and four of us keep hitting merge conflicts in it. i want to break it apart but i don't want to change any behaviour", "should_trigger": true},
  {"query": "lets build out the notifications system. email + in-app to start, slack later maybe. no idea how you'd structure it tbh, i've never touched that part of the codebase", "should_trigger": true},
  {"query": "product wants the export to also include the cancelled orders, currently we filter them out in the query. changing that changes what every existing customer sees in their exports so i want to think about it properly first", "should_trigger": true},
  {"query": "I'd like to introduce a caching layer in front of the pricing service. Redis is already in the stack for sessions. Walk me through how the pricing code is currently structured before we decide anything, I only joined last week", "should_trigger": true},
  {"query": "need to add rate limiting to the public api. per-key, sliding window, and it has to work across our 3 app servers", "should_trigger": true},
  {"query": "just quickly add a healthcheck endpoint at /healthz that returns 200 and the git sha. should take you 2 minutes", "should_trigger": true},
  {"query": "design-grill this one: i want the CSV export to stream rather than buffering the whole result set in memory. i know exactly what i want, just capture it and pressure-test it", "should_trigger": true},
  {"query": "i want to add a retry queue for the outbound webhooks in src/webhooks/, i wrote most of that package so you don't need to explain anything, and please push back on your own suggestion rather than giving me a menu", "should_trigger": true},
  {"query": "we should add proration when someone downgrades mid-cycle. i've been in app/billing for two years so keep it brisk", "should_trigger": true},

  {"query": "the export job times out on large accounts. started happening after tuesday's deploy. logs just show a 504 from the proxy, nothing in the app logs", "should_trigger": false},
  {"query": "test_invoice_rounding is failing on CI but passes locally every time. can you work out what's going on", "should_trigger": false},
  {"query": "work on #482", "should_trigger": false},
  {"query": "ok i've pushed the changes for the invite flow, is that ticket done or is there something left?", "should_trigger": false},
  {"query": "reviewer left a bunch of comments on the PR, mostly about the error handling in the webhook consumer. can you go through them", "should_trigger": false},
  {"query": "how does the retry logic in src/queue/worker.go decide the backoff interval? just trying to understand it, not changing anything", "should_trigger": false},
  {"query": "you decide on this one, i don't want to be involved. come back when you've got a design for the webhook delivery work", "should_trigger": false},
  {"query": "change the session TTL from 30 days to 7 in config/auth.yml", "should_trigger": false},
  {"query": "there's a typo in the 404 page, 'litsings' should be 'listings', it's in templates/errors/404.html", "should_trigger": false},
  {"query": "we approved the design for the notifications work last week and you've already built the email path. carry on with the in-app part", "should_trigger": false},
  {"query": "the design's approved and written up in the epic body already, go ahead and cut the child tickets for it", "should_trigger": false}
]
