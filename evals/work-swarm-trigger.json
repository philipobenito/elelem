[
  {"query": "approved design in hand for the audit trail work. run your implementation process on it, reviews and all", "should_trigger": true},
  {"query": "the notifications design is approved, email plus in-app exactly as written up. swarm it, both paths at once", "should_trigger": true},
  {"query": "we settled the streaming CSV export design earlier in this conversation and i said yes to it. parallelise the build across agents", "should_trigger": true},
  {"query": "design's signed off: parser, renderer, CLI glue, three file-disjoint chunks. fan it out across agents, size it first", "should_trigger": true},
  {"query": "the spec in docs/rate-limiting-design.md went through review last week and i approved it. orchestrate the implementation, per-task reviews included", "should_trigger": true},
  {"query": "you've recovered the design from the epic and i've confirmed the scope, so decompose it into parallel tasks and get the agents going", "should_trigger": true},
  {"query": "approved design for the invite flow. i want this swarmed rather than done one file at a time, email path and admin screen concurrently", "should_trigger": true},
  {"query": "the importer rewrite design is approved. spin up implementer agents for the disjoint bits and keep the commits to yourself", "should_trigger": true},
  {"query": "we agreed the audit log design this morning and i signed it off. run the orchestrated build on it, concurrent tasks with a review on each one", "should_trigger": true},

  {"query": "this one's dead simple, the design is approved so skip the ceremony and just bang the code out", "should_trigger": false},
  {"query": "optimise the hot path in the matcher, it's the fast path for every request and it shows up in every profile", "should_trigger": false},
  {"query": "can you do a security review of the fast path in the auth middleware before we ship it", "should_trigger": false},
  {"query": "lets build out the notifications system. email + in-app to start, slack later maybe. no idea how you'd structure it tbh", "should_trigger": false},
  {"query": "work on #42", "should_trigger": false},
  {"query": "the export job times out on large accounts since tuesday's deploy, get it fixed", "should_trigger": false},
  {"query": "the feature branch is finished, look it over before i open the PR", "should_trigger": false},
  {"query": "the design's approved and written up in the epic body already, go ahead and cut the child tickets for it", "should_trigger": false},
  {"query": "how do you decide whether a design gets decomposed into parallel tasks or done in one pass?", "should_trigger": false},
  {"query": "run the suite and tell me whether the feature is actually done", "should_trigger": false}
]
