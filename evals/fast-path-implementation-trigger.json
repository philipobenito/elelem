[
  {
    "query": "triage came back SIMPLE on the version-bump design, all six rows green. take it to fast-path-implementation",
    "should_trigger": true
  },
  {
    "query": "complexity-triage says SIMPLE (18 lines, 6 files, pure string replacement, table's already in front of the user). hand off to the fast path and stop",
    "should_trigger": true
  },
  {
    "query": "i'm overriding the COMPLEX verdict on this one. criterion 5 is the only fail and i'll take the risk. carry the failing row through so the fast path reviewer can weigh it",
    "should_trigger": true
  },
  {
    "query": "you're at sdd step 3 with a SIMPLE classification on the licence-header sweep. don't decompose it, run the batch path: one implementer, one combined review, then show me the diff before you commit anything",
    "should_trigger": true
  },
  {
    "query": "all six criteria pass on the deprecation-comment sweep across those 11 files. skip decomposition, batch it, cheapest model",
    "should_trigger": true
  },
  {
    "query": "the combined reviewer came back TRIAGE_INVALID on criterion 6, diff is 74 lines not the 30 we estimated. what happens to the uncommitted work now",
    "should_trigger": true
  },
  {
    "query": "team-driven-development triaged the config rename as SIMPLE before it spawned anyone. hand it off the same way sdd would",
    "should_trigger": true
  },
  {
    "query": "the fast path implementer just reported it added a test for the new validation branch. is that a problem",
    "should_trigger": true
  },
  {
    "query": "we're on the fast path and the reviewer has failed it twice now over the same naming inconsistency. do i escalate to sonnet or stop",
    "should_trigger": true
  },
  {
    "query": "this change is dead simple, just bump the version in package.json and the lockfile and move on. i don't need the whole ceremony for it",
    "should_trigger": false
  },
  {
    "query": "implement the fastest possible path through the router for cache hits, it's on the hot path and we're eating 12ms per request on lookups we already have the answer to",
    "should_trigger": false
  },
  {
    "query": "the fast path in our request handler skips validation when the token's cached. can you review it for security holes before this goes out friday",
    "should_trigger": false
  },
  {
    "query": "is this design simple or complex? i want to know before we start decomposing it into tasks",
    "should_trigger": false
  },
  {
    "query": "we've got an approved design for the notification service, roughly 5 tasks, mostly independent of each other. start implementing",
    "should_trigger": false
  },
  {
    "query": "i've got about 40 files that all need the same import path rewritten after the package move. what's the quickest way to get through that",
    "should_trigger": false
  },
  {
    "query": "triage returned COMPLEX. criterion 1 fails because the design mixes the docs update with the actual feature code. what now",
    "should_trigger": false
  },
  {
    "query": "just do it quickly and don't bother with a review, it's a one-line change to the timeout constant in src/config.ts",
    "should_trigger": false
  },
  {
    "query": "can you make the build faster? it's taking 4 minutes and the slow part looks like the typescript compile step in CI",
    "should_trigger": false
  }
]
